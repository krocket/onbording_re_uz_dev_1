<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vue pour l'API Web des livres -->
    <template id="api_books_template" name="API Books Template">
        <t t-call="web.layout">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <h1 class="text-center mb-4">Bibliothèque - API Livres</h1>
                        
                        <!-- Barre de recherche -->
                        <div class="row mb-4">
                            <div class="col-md-6 offset-md-3">
                                <div class="input-group">
                                    <input type="text" class="form-control" id="searchInput" placeholder="Rechercher un livre, auteur, catégorie...">
                                    <div class="input-group-append">
                                        <button class="btn btn-primary" type="button" id="searchBtn">
                                            <i class="fa fa-search"></i> Rechercher
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Filtres -->
                        <div class="row mb-3">
                            <div class="col-12 text-center">
                                <div class="btn-group" role="group">
                                    <button type="button" class="btn btn-outline-primary filter-btn" data-filter="all">Tous</button>
                                    <button type="button" class="btn btn-outline-success filter-btn" data-filter="available">Disponibles</button>
                                    <button type="button" class="btn btn-outline-warning filter-btn" data-filter="borrowed">Empruntés</button>
                                    <button type="button" class="btn btn-outline-danger filter-btn" data-filter="lost">Perdus</button>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Loading spinner -->
                        <div id="loading" class="text-center d-none">
                            <div class="spinner-border text-primary" role="status">
                                <span class="sr-only">Chargement...</span>
                            </div>
                        </div>
                        
                        <!-- Résultats -->
                        <div id="booksContainer" class="row">
                            <!-- Les livres seront affichés ici -->
                        </div>
                        
                        <!-- Pagination -->
                        <div class="row mt-4">
                            <div class="col-12 text-center">
                                <nav aria-label="Pagination des livres">
                                    <ul class="pagination justify-content-center" id="pagination">
                                        <!-- Pagination sera générée ici -->
                                    </ul>
                                </nav>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Modal pour les détails du livre -->
            <div class="modal fade" id="bookModal" tabindex="-1" role="dialog">
                <div class="modal-dialog modal-lg" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="bookModalTitle">Détails du livre</h5>
                            <button type="button" class="close" data-dismiss="modal">
                                <span>&times;</span>
                            </button>
                        </div>
                        <div class="modal-body" id="bookModalBody">
                            <!-- Contenu du modal -->
                        </div>
                    </div>
                </div>
            </div>
            
            <style>
                .book-card {
                    transition: transform 0.2s;
                    cursor: pointer;
                }
                .book-card:hover {
                    transform: translateY(-5px);
                    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
                }
                .book-image {
                    height: 200px;
                    object-fit: cover;
                }
                .state-badge {
                    position: absolute;
                    top: 10px;
                    right: 10px;
                }
                .filter-btn.active {
                    background-color: #007bff;
                    color: white;
                }
            </style>
            
            <script>
                class BooksAPI {
                    constructor() {
                        this.currentPage = 1;
                        this.itemsPerPage = 12;
                        this.currentFilter = 'all';
                        this.books = [];
                        this.init();
                    }
                    
                    init() {
                        this.bindEvents();
                        this.loadBooks();
                    }
                    
                    bindEvents() {
                        // Recherche
                        document.getElementById('searchBtn').addEventListener('click', () => {
                            this.searchBooks();
                        });
                        
                        document.getElementById('searchInput').addEventListener('keypress', (e) => {
                            if (e.key === 'Enter') {
                                this.searchBooks();
                            }
                        });
                        
                        // Filtres
                        document.querySelectorAll('.filter-btn').forEach(btn => {
                            btn.addEventListener('click', (e) => {
                                this.currentFilter = e.target.dataset.filter;
                                this.updateFilterButtons();
                                this.filterBooks();
                            });
                        });
                    }
                    
                    updateFilterButtons() {
                        document.querySelectorAll('.filter-btn').forEach(btn => {
                            btn.classList.remove('active');
                        });
                        document.querySelector(`[data-filter="${this.currentFilter}"]`).classList.add('active');
                    }
                    
                    async loadBooks() {
                        this.showLoading(true);
                        try {
                            const response = await fetch('/api/books');
                            const data = await response.json();
                            
                            if (data.success) {
                                this.books = data.data;
                                this.displayBooks();
                            } else {
                                this.showError('Erreur lors du chargement des livres');
                            }
                        } catch (error) {
                            this.showError('Erreur de connexion');
                        } finally {
                            this.showLoading(false);
                        }
                    }
                    
                    async searchBooks() {
                        const searchTerm = document.getElementById('searchInput').value.trim();
                        if (!searchTerm) {
                            this.loadBooks();
                            return;
                        }
                        
                        this.showLoading(true);
                        try {
                            const response = await fetch(`/api/books/search?q=${encodeURIComponent(searchTerm)}`);
                            const data = await response.json();
                            
                            if (data.success) {
                                this.books = data.data;
                                this.displayBooks();
                            } else {
                                this.showError(data.error || 'Erreur lors de la recherche');
                            }
                        } catch (error) {
                            this.showError('Erreur de connexion');
                        } finally {
                            this.showLoading(false);
                        }
                    }
                    
                    filterBooks() {
                        const filteredBooks = this.books.filter(book => {
                            if (this.currentFilter === 'all') return true;
                            return book.state === this.currentFilter;
                        });
                        
                        this.displayBooks(filteredBooks);
                    }
                    
                    displayBooks(booksToDisplay = null) {
                        const container = document.getElementById('booksContainer');
                        const books = booksToDisplay || this.books;
                        
                        if (books.length === 0) {
                            container.innerHTML = '<div class="col-12 text-center"><p class="text-muted">Aucun livre trouvé</p></div>';
                            return;
                        }
                        
                        const startIndex = (this.currentPage - 1) * this.itemsPerPage;
                        const endIndex = startIndex + this.itemsPerPage;
                        const paginatedBooks = books.slice(startIndex, endIndex);
                        
                        container.innerHTML = paginatedBooks.map(book => this.createBookCard(book)).join('');
                        this.updatePagination(books.length);
                    }
                    
                    createBookCard(book) {
                        const stateClass = {
                            'available': 'success',
                            'borrowed': 'warning',
                            'lost': 'danger'
                        }[book.state] || 'secondary';
                        
                        const authors = book.authors ? book.authors.map(author => author.name).join(', ') : 'Auteur inconnu';
                        const category = book.category ? book.category.name : 'Catégorie inconnue';
                        const publisher = book.publisher ? book.publisher.name : 'Éditeur inconnu';
                        
                        return `
                            <div class="col-md-4 col-lg-3 mb-4">
                                <div class="card book-card h-100" onclick="booksAPI.showBookDetails(${book.id})">
                                    <div class="position-relative">
                                        <img src="/web/image/library.book/${book.id}/image" class="card-img-top book-image" alt="${book.name}" onerror="this.src='/web/static/src/img/placeholder.png'">
                                        <span class="badge badge-${stateClass} state-badge">${book.state}</span>
                                    </div>
                                    <div class="card-body">
                                        <h5 class="card-title">${book.name}</h5>
                                        <p class="card-text text-muted small">${authors}</p>
                                        <p class="card-text text-muted small">${category}</p>
                                        <p class="card-text text-muted small">${publisher}</p>
                                        ${book.isbn ? `<p class="card-text text-muted small">ISBN: ${book.isbn}</p>` : ''}
                                    </div>
                                </div>
                            </div>
                        `;
                    }
                    
                    updatePagination(totalItems) {
                        const totalPages = Math.ceil(totalItems / this.itemsPerPage);
                        const pagination = document.getElementById('pagination');
                        
                        if (totalPages <= 1) {
                            pagination.innerHTML = '';
                            return;
                        }
                        
                        let paginationHTML = '';
                        
                        // Bouton précédent
                        paginationHTML += `
                            <li class="page-item ${this.currentPage === 1 ? 'disabled' : ''}">
                                <a class="page-link" href="#" onclick="booksAPI.changePage(${this.currentPage - 1})">Précédent</a>
                            </li>
                        `;
                        
                        // Pages
                        for (let i = 1; i <= totalPages; i++) {
                            if (i === 1 || i === totalPages || (i >= this.currentPage - 2 && i <= this.currentPage + 2)) {
                                paginationHTML += `
                                    <li class="page-item ${i === this.currentPage ? 'active' : ''}">
                                        <a class="page-link" href="#" onclick="booksAPI.changePage(${i})">${i}</a>
                                    </li>
                                `;
                            } else if (i === this.currentPage - 3 || i === this.currentPage + 3) {
                                paginationHTML += '<li class="page-item disabled"><span class="page-link">...</span></li>';
                            }
                        }
                        
                        // Bouton suivant
                        paginationHTML += `
                            <li class="page-item ${this.currentPage === totalPages ? 'disabled' : ''}">
                                <a class="page-link" href="#" onclick="booksAPI.changePage(${this.currentPage + 1})">Suivant</a>
                            </li>
                        `;
                        
                        pagination.innerHTML = paginationHTML;
                    }
                    
                    changePage(page) {
                        this.currentPage = page;
                        this.displayBooks();
                    }
                    
                    async showBookDetails(bookId) {
                        try {
                            const response = await fetch(`/api/books/${bookId}`);
                            const data = await response.json();
                            
                            if (data.success) {
                                const book = data.data;
                                this.displayBookModal(book);
                            } else {
                                this.showError('Erreur lors du chargement des détails');
                            }
                        } catch (error) {
                            this.showError('Erreur de connexion');
                        }
                    }
                    
                    displayBookModal(book) {
                        const modalTitle = document.getElementById('bookModalTitle');
                        const modalBody = document.getElementById('bookModalBody');
                        
                        modalTitle.textContent = book.name;
                        
                        const authors = book.authors ? book.authors.map(author => author.name).join(', ') : 'Auteur inconnu';
                        const category = book.category ? book.category.name : 'Catégorie inconnue';
                        const publisher = book.publisher ? book.publisher.name : 'Éditeur inconnu';
                        
                        modalBody.innerHTML = `
                            <div class="row">
                                <div class="col-md-4">
                                    <img src="/web/image/library.book/${book.id}/image" class="img-fluid" alt="${book.name}" onerror="this.src='/web/static/src/img/placeholder.png'">
                                </div>
                                <div class="col-md-8">
                                    <h6>Description</h6>
                                    <p>${book.description || 'Aucune description disponible'}</p>
                                    
                                    <h6>Informations</h6>
                                    <ul class="list-unstyled">
                                        <li><strong>Auteurs:</strong> ${authors}</li>
                                        <li><strong>Catégorie:</strong> ${category}</li>
                                        <li><strong>Éditeur:</strong> ${publisher}</li>
                                        ${book.isbn ? `<li><strong>ISBN:</strong> ${book.isbn}</li>` : ''}
                                        ${book.publication_date ? `<li><strong>Date de publication:</strong> ${book.publication_date}</li>` : ''}
                                        <li><strong>État:</strong> <span class="badge badge-${book.state === 'available' ? 'success' : book.state === 'borrowed' ? 'warning' : 'danger'}">${book.state}</span></li>
                                        ${book.loan_date ? `<li><strong>Date d'emprunt:</strong> ${book.loan_date}</li>` : ''}
                                        ${book.return_date ? `<li><strong>Date de retour:</strong> ${book.return_date}</li>` : ''}
                                    </ul>
                                </div>
                            </div>
                        `;
                        
                        $('#bookModal').modal('show');
                    }
                    
                    showLoading(show) {
                        const loading = document.getElementById('loading');
                        if (show) {
                            loading.classList.remove('d-none');
                        } else {
                            loading.classList.add('d-none');
                        }
                    }
                    
                    showError(message) {
                        const container = document.getElementById('booksContainer');
                        container.innerHTML = `
                            <div class="col-12 text-center">
                                <div class="alert alert-danger" role="alert">
                                    ${message}
                                </div>
                            </div>
                        `;
                    }
                }
                
                // Initialisation
                let booksAPI;
                document.addEventListener('DOMContentLoaded', function() {
                    booksAPI = new BooksAPI();
                });
            </script>
        </t>
    </template>
    
    <!-- Action pour ouvrir la vue API -->
    <record id="action_api_books" model="ir.actions.client">
        <field name="name">API Livres</field>
        <field name="tag">api_books_template</field>
    </record>
    
    <!-- Menu pour l'API -->
    <menuitem id="menu_api_books"
              name="API Livres"
              parent="library_menu"
              action="library.action_api_books"
              sequence="20"/>
    
    <!-- Template pour la liste des livres -->
    <template id="books_list_template" name="Books List Template">
        <t t-call="web.layout">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h1><t t-esc="title"/></h1>
                            <a href="/api/books" class="btn btn-outline-primary">
                                <i class="fa fa-home"></i> Accueil
                            </a>
                        </div>
                        
                        <!-- Barre de recherche -->
                        <div class="row mb-4">
                            <div class="col-md-6 offset-md-3">
                                <form method="GET" action="/api/books/search" class="input-group">
                                    <input type="text" name="q" class="form-control" placeholder="Rechercher un livre, auteur, catégorie..." 
                                           t-att-value="search_term or ''">
                                    <div class="input-group-append">
                                        <button class="btn btn-primary" type="submit">
                                            <i class="fa fa-search"></i> Rechercher
                                        </button>
                                    </div>
                                </form>
                            </div>
                        </div>
                        
                        <!-- Statistiques -->
                        <div class="row mb-3">
                            <div class="col-12 text-center">
                                <span class="badge badge-info">Nombre de livres: <t t-esc="count"/></span>
                            </div>
                        </div>
                        
                        <!-- Liste des livres -->
                        <div class="row">
                            <t t-if="books">
                                <t t-foreach="books" t-as="book">
                                    <div class="col-md-4 col-lg-3 mb-4">
                                        <div class="card h-100">
                                            <div class="position-relative">
                                                <img src="/web/image/library.book/{{book.id}}/image" class="card-img-top" 
                                                     style="height: 200px; object-fit: cover;" 
                                                     alt="{{book.name}}" 
                                                     onerror="this.src='/web/static/src/img/placeholder.png'">
                                                <span class="badge badge-{{'success' if book.state == 'available' else 'warning' if book.state == 'borrowed' else 'danger'}} position-absolute" 
                                                      style="top: 10px; right: 10px;">
                                                    {{book.state}}
                                                </span>
                                            </div>
                                            <div class="card-body">
                                                <h5 class="card-title">{{book.name}}</h5>
                                                <p class="card-text text-muted small">
                                                    <strong>Auteurs:</strong> 
                                                    <t t-if="book.authors and len(book.authors) > 0">
                                                        <t t-foreach="book.authors" t-as="author">
                                                            <span t-esc="author.name"/><t t-if="not author_last">, </t>
                                                        </t>
                                                    </t>
                                                    <t t-else>Auteur inconnu</t>
                                                </p>
                                                <p class="card-text text-muted small">
                                                    <strong>Catégorie:</strong> 
                                                    <t t-if="book.category">{{book.category.name}}</t>
                                                    <t t-else>Catégorie inconnue</t>
                                                </p>
                                                <p class="card-text text-muted small">
                                                    <strong>Éditeur:</strong> 
                                                    <t t-if="book.publisher">{{book.publisher.name}}</t>
                                                    <t t-else>Éditeur inconnu</t>
                                                </p>
                                                <t t-if="book.isbn">
                                                    <p class="card-text text-muted small">
                                                        <strong>ISBN:</strong> {{book.isbn}}
                                                    </p>
                                                </t>
                                            </div>
                                            <div class="card-footer">
                                                <a href="/api/books/{{book.id}}" class="btn btn-primary btn-sm">
                                                    Voir détails
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </t>
                            <t t-else>
                                <div class="col-12 text-center">
                                    <p class="text-muted">Aucun livre trouvé</p>
                                </div>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
    
    <!-- Template pour les détails d'un livre -->
    <template id="book_detail_template" name="Book Detail Template">
        <t t-call="web.layout">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h1>Détails du livre</h1>
                            <a href="/api/books" class="btn btn-outline-primary">
                                <i class="fa fa-arrow-left"></i> Retour à la liste
                            </a>
                        </div>
                        
                        <div class="row">
                            <div class="col-md-4">
                                <div class="card">
                                    <img src="/web/image/library.book/{{book.id}}/image" class="card-img-top" 
                                         alt="{{book.name}}" 
                                         onerror="this.src='/web/static/src/img/placeholder.png'">
                                    <div class="card-body text-center">
                                        <span class="badge badge-{{'success' if book.state == 'available' else 'warning' if book.state == 'borrowed' else 'danger'}} mb-2">
                                            {{book.state}}
                                        </span>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-8">
                                <div class="card">
                                    <div class="card-body">
                                        <h2 class="card-title">{{book.name}}</h2>
                                        
                                        <t t-if="book.description">
                                            <h5>Description</h5>
                                            <p class="card-text">{{book.description}}</p>
                                        </t>
                                        
                                        <h5>Informations</h5>
                                        <ul class="list-unstyled">
                                            <li><strong>Auteurs:</strong> 
                                                <t t-if="book.authors and len(book.authors) > 0">
                                                    <t t-foreach="book.authors" t-as="author">
                                                        <span t-esc="author.name"/><t t-if="not author_last">, </t>
                                                    </t>
                                                </t>
                                                <t t-else>Auteur inconnu</t>
                                            </li>
                                            <li><strong>Catégorie:</strong> 
                                                <t t-if="book.category">{{book.category.name}}</t>
                                                <t t-else>Catégorie inconnue</t>
                                            </li>
                                            <li><strong>Éditeur:</strong> 
                                                <t t-if="book.publisher">{{book.publisher.name}}</t>
                                                <t t-else>Éditeur inconnu</t>
                                            </li>
                                            <t t-if="book.isbn">
                                                <li><strong>ISBN:</strong> {{book.isbn}}</li>
                                            </t>
                                            <t t-if="book.publication_date">
                                                <li><strong>Date de publication:</strong> {{book.publication_date}}</li>
                                            </t>
                                            <t t-if="book.loan_date">
                                                <li><strong>Date d'emprunt:</strong> {{book.loan_date}}</li>
                                            </t>
                                            <t t-if="book.return_date">
                                                <li><strong>Date de retour:</strong> {{book.return_date}}</li>
                                            </t>
                                            <t t-if="book.reference">
                                                <li><strong>Référence:</strong> {{book.reference}}</li>
                                            </t>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
    
    <!-- Template pour les erreurs -->
    <template id="error_template" name="Error Template">
        <t t-call="web.layout">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <div class="text-center mt-5">
                            <h1 class="text-danger">
                                <i class="fa fa-exclamation-triangle"></i> Erreur
                            </h1>
                            <div class="alert alert-danger" role="alert">
                                <h4 class="alert-heading">Une erreur s'est produite</h4>
                                <p>{{error_message}}</p>
                            </div>
                            <a href="/api/books" class="btn btn-primary">
                                <i class="fa fa-home"></i> Retour à l'accueil
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
              
</odoo> 